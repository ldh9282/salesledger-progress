<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userns">
	<select id="select" parameterType="String" resultType="user">
		select * from t_user where username = #{id}
	</select>
	<insert id="insert" parameterType="user">
		INSERT INTO t_user (username, password, name, company, permit) VALUES (#{username}, #{password}, #{name}, #{company}, 'N');
	</insert>
	<select id="list"  resultType="user">
		select * from t_user order by authority
	</select>
	<update id="userPermit" parameterType="String">
		update t_user set permit = 'Y' where username = #{username}
	</update>
	<update id="authoritySetting" parameterType="user">
		update t_user set authority = #{authority} where username = #{username}
	</update>
</mapper>