<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="clientns">

	<resultMap type="client" id="clientMap">
		<result property="client_id" column="client_id"/>
		<result property="sales_source" column="sales_source"/>
		<result property="subcontract" column="subcontract"/>
		<result property="project_name" column="project_name"/>
		<result property="business_department" column="business_department"/>
		<result property="business_manager" column="business_manager"/>
	</resultMap>
	
	<select id="selectOne" resultMap="clientMap">
		SELECT * FROM t_client WHERE client_id = #{client_id}
	</select>
	
	<select id="findByParams" resultMap="clientMap">
		SELECT * FROM t_client 
		WHERE sales_source = #{sales_source} AND client = #{client}
		AND subcontract = #{subcontract} AND project_name = #{project_name}
		AND business_department = #{business_department} AND business_manager = #{business_manager}
	</select>
	
	<insert id="insert">
		INSERT INTO t_client
		(
			client_id, sales_source, client, 
			subcontract, project_name, business_department,
			business_manager
		)
		VALUES 
		(
			DEFAULT, #{sales_source}, #{client}, 
			#{subcontract}, #{project_name}, #{business_department}, 
			#{business_manager}
		)
		<selectKey keyProperty="client_id" keyColumn="client_id" order="AFTER" resultType="long">
			SELECT currval('t_client_client_id_seq')
		</selectKey>
	</insert>
	
	<update id="update">
		UPDATE t_client SET
			sales_source = #{sales_source}, 
			client = #{client}, 
			subcontract = #{subcontract}, 
			project_name = #{project_name}, 
			business_department = #{business_department}, 
			business_manager = #{business_manager}
		WHERE client_id = #{client_id}
	</update>	
</mapper>